// Tranche 0: animations.js
// Only subtle, purposeful motion that clarifies focus, state, or progression.
// No decorative flourishes. All motion is driven from state changes or navigation.
//
// Rules (from docs/motion-rules.md):
// - Transitions ≤ 200ms for state/selection
// - Use transform + opacity only
// - Respect prefers-reduced-motion
// - Scroll behavior is smooth via JS scrollIntoView({ behavior: 'smooth' })

export function initMotion() {
  // Placeholder for future shared motion utilities.
  // In Tranche 0 all motion is inline CSS + native scroll.
}
