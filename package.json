{
  "name": "single-canvas-workflow-wireframe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tranche 0: Single-Canvas Reactive Specimen — executable blueprint for workflow validation (not a production app)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  }
}
